@model Ecommerce.Models.ViewModels.InventoryViewModel;
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@if (Model.Id == 0)
{
    <h2>New Item</h2>
}
else
{
    <h2>Edit Item</h2>
}

<form asp-action="Save" enctype="multipart/form-data"> 
    <div>
        <label>Name:</label>
        @Html.TextBoxFor(c => c.ItemName, new { @class = "form-control"})
        @Html.ValidationMessageFor(c => c.ItemName)
        <br>
    </div>
    <div>
        <label>Description:</label>
        @Html.TextAreaFor(c => c.Description, new { @class = "form-control"})
        @Html.ValidationMessageFor(c => c.Description)
        <br>
    </div>
    <div>
        <label>Stock Remaining:</label>
        <input asp-for="StockRemaining" class="form-control"/>
        @Html.ValidationMessageFor(c => c.StockRemaining)
        <br>
    </div>
    <div>
        <label>Price:</label>
        <input asp-for="Price" class="form-control"/>
        @Html.ValidationMessageFor(c => c.Price)
        <br>
    </div>
    <div>
        <label>Image:</label>
        <input asp-for="Image" />
        <br>
    </div>
    
    @Html.HiddenFor(m => m.Id)
    <button type="submit" class="btn btn-primary">Submit</button>
</form>
    